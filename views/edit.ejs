<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Note</title>
    <link rel="stylesheet" href="stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 flex items-start justify-center py-10">
        <div class="form bg-zinc-800 w-96 rounded shadow-md">
            <form action="/edit/<%= fileName %>" method="POST" class="space-y-4 p-6">
                <div>
                    <label for="heading" class="block text-gray-200">Heading</label>
                    <input type="text" id="heading" name="title" value="<%= fileContent.title.replace('.txt', '') %>" class="w-full px-3 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-indigo-500 bg-zinc-700 text-white">
                </div>
                <div>
                    <label for="note" class="block text-gray-200">Note</label>
                    <textarea id="note" name="details" rows="6" class="w-full px-3 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-indigo-500 bg-zinc-700 text-white resize-none"><%= fileContent.details %></textarea>
                </div>
                <div>
                    <button type="submit" class="w-full bg-indigo-600 text-white py-2 rounded hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500">Update</button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
